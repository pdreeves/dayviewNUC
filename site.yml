---

 #Configure all Servers
- hosts: nuc
  become: true
  gather_facts: false

  roles:
    - baseConfig
    - formatDisk
    - docker
    - portainer
    - traefik
    - minio
    - splunk
    - universalForwarder